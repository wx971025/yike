---
description: 项目历史踩坑记录。调试 bug、修改桌面版/构建脚本/前后端、或遇到类似问题时查阅；解决新问题后在此追加条目（时间、原因、解决路线）。
alwaysApply: true
---

# 踩坑记录（Pitfalls）

记录开发过程中遇到的问题，避免重复踩坑。每条记录包含：**时间**、**原因**、**解决路线**。

## 记录格式

```markdown
### YYYY-MM-DD · 简短标题

- **原因**：
- **解决路线**：
```

## 记录

### 2026-07-24 · AI 聊天框按空格触发单词复习跳转

- **原因**：`WordReviewCard` / `ConfusablePairReviewCard` 在 `window` 上监听回车/空格确认；焦点在 AI 助手 `textarea` 或其它输入框时，按键仍会触发「下一词 / 已记住」。
- **解决路线**：新增 `shouldIgnoreGlobalShortcut()`，焦点在外部可编辑控件（非复习卡片内 `data-review-shortcut-root`）时跳过全局快捷键；复习拼写输入框仍在 root 内，答对后空格/回车仍可切词。

### 2026-07-24 · 云端同步未带复习配置与乱序种子

- **原因**：数据导出/同步只含分组、单词等学习数据；每日上限、乱序模式、UI 偏好存 localStorage 或 users 表未写入 export；乱序种子原先由客户端 `hash(userId+日期)` 生成，跨设备 user_id 不同导致顺序不一致。
- **解决路线**：备份 `version=2` 增加 `settings`（`word_review_daily_cap`、`word_review_order_mode`、`review_ui_prefs`）与 `word_review_daily_batches`（含 `shuffle_seed`）；users 表持久化复习配置；前端 `scheduleReviewSettingsSync` 写回服务端，Dashboard 用 API 返回的 `shuffle_seed` 乱序；导入/拉取后 `authApi.me()` 应用配置。Web 已 deploy backend+frontend。


- **原因**：两个叠加缺陷。①`AiConfigUpdate.title`/`AiConfigCreate.title` 有 `min_length=1`，且更新处理器还有 `if not config.title: 400`，导致空标题配置在「测试连通性」前的保存（`PUT /auth/ai-configs/{id}`）返回 **422**（title 只是展示标签，列表本就用「未命名配置」兜底，不该必填）；前端 `canSubmit` 也把 title 设为必填，空标题时测试按钮直接 disabled → 点击「无反应」。②前端 `setError(err.response?.data?.detail ?? ...)` 未处理 FastAPI 422 的 `detail` 是**数组**（`[{msg,loc}]`），`setError(数组)` 后 `<p>{error}</p>` 渲染对象数组 → React 崩溃/空白，错误彻底不显示，表现为「无反应」。
- **解决路线**：后端 title 改为可选（去掉 `min_length`，`AiConfigCreate.title` 默认 `""`，更新处理器删掉 title 的 400 校验，仍校验 base_url/api_key/model）；前端加 `extractErrorMessage()` 把字符串/数组 `detail` 统一提取为可读文案并用于所有 catch，`canSubmit`/`handleSave` 不再要求 title。已用真实 HTTP 复现并验证 create-no-title→201、PUT 空 title→200。Web 已 deploy backend+frontend，桌面随 v1.1.15 发版。

### 2026-07-23 · AI 配置测试/保存提示一闪即没（loadConfigs/resetForm 清空反馈）

- **原因**：`handleTest` 失败时 `setError` 后立刻 `await loadConfigs()`，而 `loadConfigs` 开头 `setError("")` 把错误清掉；成功时 `setMessage` 后 `resetForm()` 又把 `message` 清掉。用户看到「没有任何提示」。
- **解决路线**：`loadConfigs({ preserveFeedback: true })` 与 `resetForm({ clearFeedback: false })` 在测试/保存成功路径保留反馈；失败路径先 `loadConfigs(preserveFeedback)` 再 `setError`。v1.1.16 发版。

### 2026-07-23 · macOS 桌面版下载词典 CERTIFICATE_VERIFY_FAILED

- **原因**：PyInstaller 打包的 macOS Python 无系统 CA 根证书（OpenSSL `load_default_certs` 在 app bundle 内找不到证书路径），所有 HTTPS（词典下载 `urlretrieve`、更新检查、镜像 `urlopen`）报 `SSL: CERTIFICATE_VERIFY_FAILED unable to get local issuer certificate`；Windows 因 Python 会读系统证书存储而正常。
- **解决路线**：`mac-desktop/launcher/entry.py` 在 `main()` 早期 `_configure_ca_bundle()` 把 `SSL_CERT_FILE`（+`REQUESTS_CA_BUNDLE`）指向 `certifi.where()`，单进程内覆盖后端与启动器所有 urllib/ssl 请求（仍做证书校验，安全）；`requirements-build.txt` 显式加 `certifi`，`yike-mac.spec` 加 `certifi` hiddenimport + `collect_all` 打包 `cacert.pem`。v1.1.14 发版。

### 2026-07-23 · 全新数据库首启崩溃（迁移顺序：reminders 表未建就 ALTER）

- **原因**：`backend/app/main.py` 迁移调用顺序里，`migrate_reminder_group_id_v1()`（`ALTER TABLE reminders ADD COLUMN group_id`）和 `migrate_assign_default_groups_v1()` 排在创建表的 `migrate_reminders_v1()` 之前。`reminders` 不是 ORM 模型（不由 `create_all` 建），只由该迁移建表；老库早已有表故无感，但**全新空库**（桌面版每次首装、Web 全新部署）会 `sqlite3.OperationalError: no such table: reminders`，后端线程崩溃→健康检查不过→桌面版进程在 Dock 但不弹窗。
- **解决路线**：把 `migrate_reminders_v1()` 提到 `migrate_reminder_group_id_v1()` 之前（各迁移由 `schema_meta`/列存在性守护，重排对老库幂等安全）。已在容器内用全新 sqlite 验证 `import app.main` 不再报错。v1.1.13 发版。

### 2026-07-23 · 新增 macOS 桌面版（mac-desktop）

- **原因**：需在 Windows 版之外提供 Mac 原生 .app/.dmg；Linux 无法交叉编译 macOS 应用，pywebview 在 Mac 用 WKWebView(cocoa/pyobjc) 而非 WebView2/pythonnet，`pystray` 托盘与 cocoa 主线程 run loop 冲突。
- **解决路线**：新建 `mac-desktop/`（镜像 `windows-desktop/` 结构），前端/后端零改动，通过 `patches/` 套用到 `workspace/`；`launcher/entry.py` 数据目录改 `~/Library/Application Support/YiKe/`、日志 `~/Library/Logs/YiKe/`、崩溃用 `osascript` 弹窗、cocoa 后端、不做托盘（关窗即退出）；`packaging/yike-mac.spec` 用 `BUNDLE` 产 .app + `collect_all(webview/objc/WebKit/...)`；`build.sh` 打包后 `codesign --force --deep --sign -` ad-hoc 签名 + `hdiutil` 生成 `YiKe-mac-arm64.dmg`；CI `build-mac-desktop.yml` 跑 `macos-latest`(arm64)。更新仅「检查更新→下载 dmg→`open` 挂载」，非静默自动安装。首启若被 Gatekeeper 拦截需右键打开或 `xattr -dr com.apple.quarantine`。待实体机构建验证（占位，构建中补充）。

### 2026-07-22 · 桌面版新手引导每次打开都出现

- **原因**：pywebview/WebView2 未设置 `storage_path` 时 localStorage 不持久；桌面版 token 有 bootstrap 兜底所以仍免登录，但引导完成标记只写 localStorage，重启后丢失。
- **解决路线**：`entry.py` 为 WebView 指定 `%LOCALAPPDATA%\\YiKe\\webview`；引导完成同时写入 `desktop_prefs.json`（`/api/desktop/preferences/onboarding/{user_id}`）；启动时从服务端 hydrate 到 localStorage。

### 2026-07-22 · Windows 桌面快捷方式图标不更新

- **原因**：`icon.ico` 与 `logo.png` 曾需手动同步；PyInstaller `build/` 缓存导致 exe 内嵌图标未刷新；Inno 桌面快捷方式默认不勾选且未指定 `IconFilename`，Windows 会缓存旧快捷方式图标。
- **解决路线**：`build.ps1` 每次从 `logo.png` 生成 `icon.ico` 并 `--clean` 打包；安装目录附带 `icon.ico`，快捷方式显式 `IconFilename`；升级时 `checkedonce` 重建桌面快捷方式。

### 2026-07-22 · 数据导出/导入（跨平台迁移）

- **原因**：用户换平台需完整迁移分组/事项/单词/易混词/提醒/技能，含学习日期与各记忆轨道轮次。
- **解决路线**：`routers/data_transfer.py` 用 SQLAlchemy `inspect` 通用序列化所有列（自动含全部日期与 stage 字段），导入时新建记录并重映射 `group_id`、`source_word_id`；支持 `mode=merge/replace`。前端 `dataApi` + 左下角设置菜单「导出/导入数据」，导入后派发 `app-data-changed` 刷新。PyInstaller spec 需补 `app.routers.data_transfer` hiddenimport。

### 2026-07-22 · uvicorn 在 windowed exe 下 `sys.stdout` 为 None 崩溃

- **原因**：`console=False` 打包后 `sys.stdout/stderr` 为 `None`，uvicorn 默认日志的 `ColourizedFormatter.__init__` 调用 `sys.stdout.isatty()` 抛 `AttributeError`，导致 `configure_logging` 失败、后端起不来、健康检查超时、窗口打不开。
- **解决路线**：`uvicorn.run(..., log_config=None)` 禁用其自带日志；启动最早期 `_ensure_std_streams()` 把 `None` 的 stdout/stderr 替换为带 `isatty/write/flush` 的空流兜底。

### 2026-07-22 · 桌面版 exe 双击打不开（静默崩溃）

- **原因**：`console=False` 时任何未捕获异常都静默退出；pywebview 的 WebView2/pythonnet 原生依赖在 onedir 打包中收集不全，`webview.start(gui="edgechromium")` 直接抛异常导致进程退出，用户看不到任何提示。
- **解决路线**：`entry.py` 顶层 `try/except` 捕获所有异常，写 `crash.log` 并弹 MessageBox；原生窗口按 edgechromium→cef→mshtml→默认 逐个降级，全失败则用 Edge `--app` 或默认浏览器兜底并阻塞保活；`yike.spec` 用 `collect_all` 收集 `webview`/`clr_loader`/`pythonnet`；`requirements-build.txt` 显式加 `pythonnet`。

### 2026-07-22 · build.ps1 首行 UTF-8 BOM 导致 PowerShell 5.1 把注释当代码

- **原因**：文件带 BOM 时首行 `# ... (PyWebview + ...)` 的 `#` 不被识别为注释，括号内 `PyWebview` 被当作命令；`param` 也不再是脚本首语句而报错。
- **解决路线**：`param(...)` 必须放在文件第一行；脚本保存为 **UTF-8 无 BOM**、纯 ASCII；注释中避免括号。

### 2026-07-22 · sync-source robocopy exit 16

- **原因**：只拷贝了 `windows-desktop/` 到 `Downloads` 等目录，上级没有同级 `backend/`、`frontend/`，robocopy 找不到源目录返回 16（Serious error）。
- **解决路线**：在完整仓库内执行 `repo\windows-desktop\build.ps1`；或设置 `$env:YIKE_SOURCE_ROOT` 指向仓库根；或 Linux 预构建后连同 `workspace/` 拷到 Windows 并 `.\build.ps1 -SkipSync`。`sync-source.ps1` 已增加路径检测与明确报错。

### 2026-07-22 · build.ps1 在 Windows PowerShell 5.1 解析失败

- **原因**：`"($sizeMb MB)"` 中 `$()` 子表达式里的 `MB` 被当作 PowerShell 大小单位后缀导致 ParserError；脚本为 UTF-8 无 BOM 时中文乱码还可能破坏字符串引号。
- **解决路线**：改用 `("{0} ({1} MB)" -f $setup, $sizeMb)` 输出体积；`build.ps1`/`sync-source.ps1` 保存为 UTF-8 BOM，构建日志改用 ASCII 英文避免编码问题。

### 2026-07-22 · Windows 桌面版改用 PyWebview onedir + 词典按需下载

- **原因**：旧 WinUI 单文件方案启动失败多、词典内置导致安装包过大；用户需要双击图标即用、免登录、词典按需下载。
- **解决路线**：新建 `windows-desktop/`：`PyWebview` 原生窗口 + `PyInstaller onedir` + Inno 递归安装；`patches/` 注入免登录 bootstrap 与词典下载管理页；`dict_setup` 在 `YIKE_DESKTOP=1` 时禁止自动后台下载；CI 工作流 `build-windows-desktop.yml`。

### 2026-07-22 · 安装后 YiKe 打不开

- **原因**：`PublishSingleFile` 的 WinUI 程序在部分环境静默失败；`OnLaunched` 里 await 后端失败无 UI 提示；PyInstaller 后端首次解压超过 60s 超时；安装包只复制了单个 `YiKe.exe` 缺少 WinUI 依赖 DLL。
- **解决路线**：改为多文件 publish + Inno 递归安装 `stage/*`；增加 StartupPage 与 `%LOCALAPPDATA%\YiKe\logs\` 后端日志；启动超时改为 3 分钟。

### 2026-07-22 · Inno Setup CI 缺少 ChineseSimplified.isl

- **原因**：GitHub `windows-latest` 自带的 Inno Setup 6 未包含 `Languages\ChineseSimplified.isl`，`yike.iss` 指定简体中文语言包会直接编译失败。
- **解决路线**：安装脚本改用 `compiler:Default.isl`（英文向导）；应用名、开始菜单项等自定义字符串仍可写中文。

### 2026-07-21 · WinUI PublishSingleFile 要求 EnableMsixTooling

- **原因**：Windows App SDK 1.6 在 `PublishSingleFile=true` 时需要生成 embedded `resources.pri`，`EnableMsixTooling=false` 会直接报错。
- **解决路线**：`YiKe.Desktop.csproj` 设 `EnableMsixTooling=true`，同时保持 `WindowsPackageType=None`（仍是免 MSIX 安装的 unpackaged exe）。

### 2026-07-21 · Windows 原生客户端只能在 Windows 上完整构建

- **原因**：WinUI 3 + PyInstaller 打包依赖 Windows SDK / VS 2022；Linux 开发机只能 `sync-source` 与维护源码，无法本地产出 `YiKe.exe` / `YiKeSetup.exe`。
- **解决路线**：在 `windows-client/` 维护源码与 `build.ps1`；用 GitHub Actions `build-windows-client.yml`（`windows-latest`）产出 artifact；实体机构建需安装 Python 3.11、.NET 8 SDK、Windows App SDK、Inno Setup 6。

### 2026-07-21 · 逾期复习后单词立刻又出现在待复习

- **原因**：`mark_reviewed` 只推进 `stage_index`，未按实际复习日重算 `learned_at`；下一轮到期日 `learned_at + days[stage]` 仍落在过去，复习完当天 `is_due` 仍为 true。双轨上线后，拼写复习完还会看到认知轨待复习（设计如此，但叠加上述 bug 更像「又出来一遍」）。
- **解决路线**：复习推进轮次时用「复习日 + 阶段间隔差」重算 `learned_at`；一次性迁移 `fix_late_review_schedule_v1` 修复历史数据。

### 2026-07-14 · 单词复习切换时整页闪一下

- **原因**：`app-data-changed` 触发 Dashboard `load()` 时 `setLoading(true)`，待复习区短暂变成「加载中…」；同时 `words` 更新会重新 `orderWords` 乱序洗牌，进度条与控制栏跟着重绘。
- **解决路线**：后台刷新用 `load({ silent: true })` 跳过 loading；复习队列仅在切换乱序/分组时重排，平时只剔除已完成单词；内容区淡出时保留 `min-height` 避免布局跳动。

<!-- 在此追加新条目，最新的放在最上面 -->

### 2026-07-23 · 应用内更新「安装包大小校验失败」

- **原因**：下载完成后用 GitHub API 的 `asset_size` 与本地文件做严格比对；网络中断导致文件不完整，或 HTTP `Content-Length` 与 API 大小不一致时误报。
- **解决路线**：以实际下载字节数 + `Content-Length` 校验；检查 PE 头 `MZ`；失败重试 3 次、超时 300s。v1.1.4 发版。

### 2026-07-23 · Release CI 未生成 YiKeSetup.exe

- **原因**：`yike.iss` 中 `procedure KillYiKeProcesses()` 空括号不符合 Inno Pascal 语法，ISCC 编译失败；`build.ps1` 未检查 ISCC 退出码，构建步骤仍显示成功。
- **解决路线**：改为 `procedure KillYiKeProcesses;`；`build.ps1` 在 ISCC 非零或未产出安装包时 `throw`；移除无效的 `AllowMultipleInstances` 指令。v1.1.3 发版。

<!-- 在此追加新条目，最新的放在最上面 -->

<!-- 在此追加新条目，最新的放在最上面 -->

<!-- 在此追加新条目，最新的放在最上面 -->

### 2026-07-23 · 安装包向导标题版本号始终 1.0.1

- **原因**：`yike.iss` 硬编码 `#define MyAppVersion "1.0.1"` 在 Inno 预处理中会覆盖 `ISCC /DMyAppVersion=` 传入的值。
- **解决路线**：改为 `#ifndef MyAppVersion` 条件定义；`build.ps1` 默认版本优先读 `launcher/version.json`。v1.1.8 发版。

### 2026-07-23 · Web 导入后分组显示「未知分组」

- **原因**：导入成功后 `app-data-changed` 会刷新卡片/单词列表，但 `GroupContext` 未监听该事件，分组列表仍是导入前的缓存；条目 `group_id` 已指向新分组，前端找不到对应分组名。
- **解决路线**：`GroupContext` 在 `app-data-changed` 时调用 `refreshGroups()`；导入时 `group_map` 统一用 int 规范化 id。

### 2026-07-23 · 桌面版更新走自建镜像 releases/desktop

- **原因**：国内用户直连 GitHub API / Release 下载易超时；服务器 43.128.141.141:80 可稳定访问 GitHub。
- **解决路线**：`scripts/sync-desktop-release.sh` 将 `YiKeSetup.exe` 同步到 `releases/desktop/`（gitignore）；Nginx `/releases/desktop/` 静态托管；桌面版优先读 `latest.json` 与镜像下载，失败回退 GitHub。v1.1.7 发版。

### 2026-07-23 · 首次安装 ucrtbase.dll 仍失败（v1.1.6 二次修复）

- **原因**：Win10+ 实际加载系统 UCRT，但 PyInstaller 仍把 `ucrtbase.dll` 打进 `_internal`；Inno **SolidCompression** 批量 MoveFile 时易被杀毒/索引锁住。用户装到 `D:\YiKe` 时，`PrivilegesRequired=lowest` 也可能加剧写入失败。
- **解决路线**：`yike.spec` 过滤 `ucrtbase.dll` 与 `api-ms-win-crt/core*`；Inno `SolidCompression=no`、`PrivilegesRequired=admin`、`[InstallDelete]` 清 `_internal`、安装前 `rd` + 等待 5s。v1.1.6 发版。

### 2026-07-23 · 首次安装 ucrtbase.dll MoveFile code 5（非仅应用内更新）

- **原因**：用户反馈为**第一次安装**到 `D:\YiKe` 即失败。除「忆刻仍在托盘占用 `_internal`」外，更常见的是：PyInstaller **UPX 压缩** CRT/Python DLL 触发杀毒扫描并短暂锁文件；或目标目录已有**上次失败残留** / 用户曾从同路径运行便携版 `YiKe.exe`。
- **解决路线**：`yike.spec` 关闭 UPX；Inno `[Files]` 加 `restartreplace`、`SetupMutex`；`PrepareToInstall` 里 `taskkill` + 等待数秒（含 `_internal` 已存在时再等）；`InitializeSetup` 检测 `YiKeDesktopMutex` 并提示退出。v1.1.2 发版。

### 2026-07-23 · 应用内更新安装时 ucrtbase.dll 拒绝访问

- **原因**：旧逻辑先 `POST /update/install` 启动 Inno，再退出应用；YiKe 仍在托盘运行，PyInstaller `_internal` 下 DLL 被占用，MoveFile 失败 code 5。
- **解决路线**：`run_update_installer` 先写 `%TEMP%\YiKe\run-update-*.cmd` 延迟 3 秒启动安装包，再立即 `_shutdown_desktop_app()`；Inno 增加 `CloseApplications=force`、`AppMutex=YiKeDesktopMutex` 与 `PrepareToInstall` 里 `taskkill /IM YiKe.exe /F`。

### 2026-07-23 · 桌面版应用内更新需先退出进程

- **原因**：YiKe 关闭窗口会隐藏到托盘，后端与 WebView 进程仍占用安装目录；Inno 覆盖文件时可能失败或需用户手动结束进程。
- **解决路线**：安装器使用 `/CLOSEAPPLICATIONS`；前端「立即安装」先调 `POST /api/desktop/update/install` 再 `pywebview.api.quit_for_update()` 退出托盘与窗口；用户数据在 `%LOCALAPPDATA%\YiKe\` 不受安装覆盖影响。

### 2026-07-23 · GitHub Releases API 限流

- **原因**：未认证请求 GitHub API 约 60 次/小时/IP；启动检查 + 手动检查频繁可能触发 403。
- **解决路线**：启动后每天最多提示一次（`desktop_prefs.json` 的 `last_check_at`）；检查失败时静默或设置页提示，不阻断正常使用；必要时后续可加 `GITHUB_TOKEN` 或自建更新元数据。

### 2026-07-23 · pywebview 5.3 无 FileDialog 枚举

- **原因**：项目锁定 `pywebview==5.3.2`，该版本只有 `OPEN_DIALOG` / `FOLDER_DIALOG` / `SAVE_DIALOG` 常量；直接使用 `webview.FileDialog.FOLDER` 会报 `module 'webview' has no attribute 'FileDialog'`。
- **解决路线**：launcher 用 `_dialog_type()` 兼容新旧 API；5.3 走 legacy 常量，6.x 走 `FileDialog` 枚举。

### 2026-07-23 · 桌面版数据导出 js_api 传大 JSON 失败

- **原因**：前端把完整备份 JSON 经 `window.pywebview.api.save_export(content, …)` 传给 Python，数据稍大或桥接异常时会失败；另保存对话框在非 GUI 线程调用也可能报错。
- **解决路线**：Python 端改从 `http://127.0.0.1:{YIKE_PORT}/api/desktop/data/export` 拉取数据再写盘；新增 `/api/desktop/data/export/save` 作为无 js_api 时的后备（直接保存到「下载」）；对话框失败时自动降级到 Downloads。

### 2026-07-23 · 桌面版数据导出无反应

- **原因**：WebView2 内 `<a download>` + Blob URL 不会触发系统保存对话框，点击导出看似成功但文件不会落盘。
- **解决路线**：launcher 通过 `js_api.save_export` 调原生 `FileDialog.SAVE` 写文件；前端桌面模式走 `window.pywebview.api.save_export`，Web 版仍用浏览器下载。

### 2026-07-23 · 桌面版关闭窗口即退出

- **原因**：pywebview 默认关闭窗口会销毁进程；后端在 daemon 线程，主线程退出后一并结束。
- **解决路线**：集成 `pystray` 系统托盘；`window.events.closing` 拦截关闭并 `hide()`；托盘菜单提供「打开忆刻 / 退出」，仅「退出」才真正 `destroy()` 结束进程。

### 2026-07-23 · Web 与桌面进度云端同步

- **原因**：Web 与 Windows 本地 SQLite 各自独立，仅靠 JSON 文件手动导入导出不便。
- **解决路线**：`users.sync_code`（UUID，注册或迁移时生成，不可改删）；Web 展示同步码；桌面输入同步码后 `POST /api/desktop/data/sync/push|pull` 代理至云端 `YIKE_CLOUD_API_BASE`（默认 `http://43.128.141.141`）；push/pull 均先清空目标端再以 replace 模式导入。

### 2026-07-23 · Web 同步码复制失败

- **原因**：站点走 HTTP 时 `navigator.clipboard.writeText` 不可用（仅 secure context 即 HTTPS/localhost）。
- **解决路线**：`copyTextToClipboard` 先尝试 Clipboard API，失败则 `document.execCommand('copy')`；同步码改为可选中 input，点击即可全选手动 Ctrl+C。

### 2026-07-23 · 每日单词复习上限

- **需求**：每日复习单词过多时可设上限，最小 10，无最大值；默认不限。
- **解决路线**：`users.word_review_daily_cap`（NULL=不限）；`GET /reviews/today/words` 按逾期排序后截断；拼写/认知轨各自独立计数；设置入口在「今日复习 → 单词 → 复习设置」。

### 2026-07-24 · 复习进度突然回到起点（如 50/50）

- **原因**：每答完一词触发 `app-data-changed` 全量重拉待复习列表；`mergeWordReviewQueue` 在 `kept` 为空时会 `resetSession`（清空 history、重置 session total），或把已在本地完成的词当作 additions 塞回队列，进度公式 `total - queueLen + 1` 回到 1。
- **解决路线**：完成/跳过仅 `loadCompleted()` 不再全量刷新；会话内 `sessionDoneIds` 禁止已做题 re-enqueue；merge 在 `prev` 非空时不 reset；进度改为 `wordHistory.length + 1` 与 `max(sessionTotal, history + queue)`。

### 2026-07-24 · 部署后无法登录（后端 ImportError 崩溃）

- **原因**：`word_daily_batch.py` 误从 `memory_schedule` 导入 `resolve_memory_mode`，该函数实际在 `review.py`；后端启动失败，所有 API（含登录）不可用。
- **解决路线**：改为 `from ..services.review import get_due_date, resolve_memory_mode` 并重新 `./deploy.sh backend`。

- **原因**：`GET /reviews/today/words` 每次从当前待复习池截前 N 个；答完若干后刷新会从剩余池再取 N 个。前端乱序用 `Math.random()` 无种子。
- **解决路线**：`word_review_daily_batches` 锁定当日批次；API 返回 `{ words, batch_total }`；前端 `orderReviewWords` 用确定性种子；乱序/顺序 preference 存 localStorage。

### 2026-07-23 · 调高每日复习上限后队列仍只有 10 个
- **解决路线**：`mergeWordReviewQueue` 在保留已排队单词的同时 append 新增 id；并更新 session total ref。

### 2026-07-24 · 答对后进度条不前进

- **原因**：防误触修复把 450ms 锁定加在 `advanceAfterCorrect` 上，回车确认答对后第二次回车推进也被拦截；进度条按「当前词序号」计算，答对后未点「下一个」前数字不变，像没动。
- **解决路线**：拼写答对后进度条即时 +1 显示（`displayProgressStep`）；锁定仅保留在鼠标点击「下一个/熟知」等按钮，键盘回车可正常推进；完成复习改用函数式 `setQueue` 更新队列。

### 2026-07-24 · 显示答案/释义后立刻跳到下一词

- **原因**：①查看拼写答案会 `resetStage` 并触发 `app-data-changed`，队列被后台刷新打乱；②显示释义或答对后按钮瞬间替换（「显示释义」→「熟知」、「确认」→「下一个」），同一次点击/回车会误触下一词。
- **解决路线**：peek 重置仅本地 patch 队列，不再 dispatch `app-data-changed`；WordReviewCard 在显示释义/答对后 450ms 内锁定鼠标点击「下一个/熟知」等按钮（键盘回车不受限）。
