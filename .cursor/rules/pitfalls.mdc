---
alwaysApply: false
---

# 踩坑记录（Pitfalls）

记录开发过程中遇到的问题，避免重复踩坑。每条记录包含：**时间**、**原因**、**解决路线**。

## 记录格式

```markdown
### YYYY-MM-DD · 简短标题

- **原因**：
- **解决路线**：
```

## 记录

### 2026-07-14 · 单词复习切换时整页闪一下

- **原因**：`app-data-changed` 触发 Dashboard `load()` 时 `setLoading(true)`，待复习区短暂变成「加载中…」；同时 `words` 更新会重新 `orderWords` 乱序洗牌，进度条与控制栏跟着重绘。
- **解决路线**：后台刷新用 `load({ silent: true })` 跳过 loading；复习队列仅在切换乱序/分组时重排，平时只剔除已完成单词；内容区淡出时保留 `min-height` 避免布局跳动。

<!-- 在此追加新条目，最新的放在最上面 -->
